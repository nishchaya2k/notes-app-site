/**
============================================================
📘 SEMANTIC HTML TAGS — QUICK REFERENCE GUIDE
============================================================

<header>
- Represents the top/intro section.
- Use for page titles, logos, navigation bars, or section headers.

<main>
- The main unique content of the page.
- Use once per page; excludes header, footer, sidebars.

<section>
- Groups related content under a meaningful heading.
- Example: Notes list, Dashboard stats, Settings groups.

<article>
- Self-contained, independent content.
- Example: A single note, blog post, comment, card item.

<aside>
- Secondary/side content.
- Example: Sidebars, ads, related links, filters.

<footer>
- Represents bottom content of a page or section.
- Use for copyright, contact info, form actions.

<nav>
- Section containing navigation links.
- Example: top navbar, sidebar nav, breadcrumb links.

<form>
- Collects user input.
- Should wrap all input fields belonging to a single action.

<figure> & <figcaption>
- Use when displaying media with a caption.
- Example: image with explanation.

============================================================
💡 SIMPLE RULES
============================================================
✔ Use <article> when the content can stand alone.
✔ Use <section> to group related content WITH a heading.
✔ Use <header> and <footer> inside ANY component, not only pages.
✔ Use <main> only once.
✔ Use <aside> only for supporting/extra information.
✔ Avoid using <div> when a semantic tag fits better.

============================================================
**/
